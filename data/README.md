# Datos

Solo se guardan ejemplos ligeros y reproducibles. Las grabaciones de cámara y logs crudos deben mantenerse fuera del repositorio o en almacenamiento externo; `data/raw/` está excluido por `.gitignore`.
